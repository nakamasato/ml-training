name: flake8

on: [pull_request]

jobs:
  flake8:
    runs-on: ubuntu-latest
    name: Lint
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: "3.9"
      - name: flake8
        uses: py-actions/flake8@v2
